import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.minhacolecao.app',
  appName: 'Minha Coleção',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
